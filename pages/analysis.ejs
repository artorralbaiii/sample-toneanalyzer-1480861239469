<!DOCTYPE html>
<html>

<head>
    <title>Tone Analyzer</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="stylesheets/style.css">
</head>

<body>

    <main class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <h2>WATSON - Tone Analyzer</h2> <a href="/" class="btn btn-warning">New Analysis*</a>
                <br>
                <br>

                <% for(var i=0; i<analysis.length; i++) {%>
                    <div class="panel panel-success">
                        <div class="panel-heading">
                            <h3 class="panel-title"><%= analysis[i].category_name %></h3>
                        </div>

                        <!-- Table -->
                        <table class="table">

                            <% for (t in analysis[i].tones) { %>
                                <tr>
                                    <td><%= analysis[i].tones[t].tone_name %></td>
                                    <td><%= analysis[i].tones[t].score %></td>
                                </tr>
                            <% } %>
                        </table>

                    </div>
                <% } %>

            </div>
        </div>
        
        
    </main>

</body>

</html>